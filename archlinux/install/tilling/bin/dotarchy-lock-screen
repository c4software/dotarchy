#!/bin/bash

# Lock the screen using swaylock
pidof swaylock || swaylock --screenshot --effect-blur 7x5 --effect-vignette 0.5:0.5 &

# Avoid running screensaver when locked
pkill -f "alacritty --class Screensaver"